[Unit]
Description = prometheus
Wants = network.target
After = network.target

[Service]
ExecStart = {{pmt_dir}}/prometheus-{{pmt_vers}}.{{pmt_os}}-{{pmt_arch}}/prometheus --config.file={{pmt_dir}}/prometheus-{{pmt_vers}}.{{pmt_os}}-{{pmt_arch}}/prometheus.yml

[Install]
WantedBy = multi-user.target
